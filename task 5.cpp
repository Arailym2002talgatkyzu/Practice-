#include <cmath>
#include <iostream>

using namespace std;
int main() {
	
	for (int a=3, i = 2; a<999,i <= sqrt(a+i); a++, ++i) {
		if (a % i != 0) {
			cout <<a<<" "<<a+2 << endl;
			
		}
	return 0;}
	return 0;
}